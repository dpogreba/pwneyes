*.iml
.gradle
/local.properties
/.idea
.DS_Store
/build
/captures
.externalNativeBuild
.cxx
local.properties

# Android build outputs
app/build/

# Kotlin
*.class

# Gradle wrapper jar (downloaded at build time)
gradle/wrapper/gradle-wrapper.jar

# Claude Code session files
.claude/

# Signing material — NEVER commit. Keystores and their passwords live only in
# GitHub Secrets / your offline backup. Losing the key means no future in-place
# updates are possible.
*.jks
*.keystore
*.b64
keystore.properties
