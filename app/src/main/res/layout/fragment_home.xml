<?xml version="1.0" encoding="utf-8"?>
<!-- Home fragment: BT connection list + FAB.
     RecyclerView scrolls naturally when items overflow the screen;
     when the list is empty the empty-state view is shown instead. -->
<androidx.coordinatorlayout.widget.CoordinatorLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <!-- Empty state — shown when connection list is empty -->
    <LinearLayout
        android:id="@+id/empty_state"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:gravity="center"
        android:orientation="vertical"
        android:padding="@dimen/empty_state_padding"
        android:visibility="visible">

        <ImageView
            android:layout_width="@dimen/empty_state_icon_size"
            android:layout_height="@dimen/empty_state_icon_size"
            android:alpha="0.4"
            android:contentDescription="@string/cd_bluetooth_icon"
            android:src="@drawable/ic_bluetooth"
            app:tint="@color/blue_500" />

        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginTop="16dp"
            android:text="@string/no_connections_title"
            android:textSize="18sp"
            android:textStyle="bold" />

        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginTop="8dp"
            android:gravity="center"
            android:text="@string/no_connections_message"
            android:textColor="?attr/colorOnSurface"
            android:alpha="0.6"
            android:textSize="14sp" />

    </LinearLayout>

    <!-- Connections list — shown when at least one connection exists.
         Infinite scroll is provided automatically by RecyclerView
         when content height exceeds the screen; no extra code needed. -->
    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/recycler_connections"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:clipToPadding="false"
        android:paddingBottom="80dp"
        android:scrollbars="vertical"
        android:visibility="gone"
        app:layoutManager="androidx.recyclerview.widget.LinearLayoutManager" />

    <!-- Circular floating action button to add a new BT connection -->
    <com.google.android.material.floatingactionbutton.FloatingActionButton
        android:id="@+id/fab_add"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="bottom|end"
        android:layout_margin="@dimen/fab_margin"
        android:contentDescription="@string/fab_add_connection"
        android:src="@drawable/ic_add"
        app:fabSize="normal"
        style="@style/Widget.PwnEyes.FAB" />

</androidx.coordinatorlayout.widget.CoordinatorLayout>
